TALIH-PD1 Kernel (tb8788p1_64_wifi, MT6771)
==========================================

Chuwi HiPad Plus / Swaiot AIP10A / 读书郎 C13 Pro / SUPI T11 等
tb8788p1_64_wifi 公版平板的内核源码 (MediaTek MT6771 / Helio P60, PMIC MT6358)。

- 内核版本: 4.14.186 (与原厂一致, vermagic 匹配原厂 vendor 模块)
- 基础源码: ubuntu-touch-unihertz-titan/kernel-alps-mt6771 (alps-mp-r0 BSP)
- 编译链: clang 11.0.1 r383902 (与原厂构建版本相同)


构建 (GitHub Actions)
---------------------

    git push origin main          # push 即触发构建
    # 或网页: Actions -> build-kernel -> Run workflow

产物: Actions run 的 artifact Image-TALIH-PD1 (arch/arm64/boot/Image + vmlinux + System.map)。

本地构建 (需 clang r383902 + gcc-aarch64-linux-gnu):

    export PATH="<toolchain>/clang-r383902b/bin:$PATH"
    make ARCH=arm64 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- \
         CROSS_COMPILE=aarch64-linux-gnu- -j10 Image dtbs
    # KBUILD_KCONFIG 需指向 arch/arm64/Kconfig (MTK 惯例, 顶层无 Kconfig)

产物: arch/arm64/boot/Image (约 25MB), arch/arm64/boot/dts/mediatek/mt6771.dtb。


打包 boot.img 并刷入
--------------------

    ./pack_boot.sh                          # 生成 ../boot_test.img (原厂 header+ramdisk+dtb + 新内核)
    adb reboot bootloader
    fastboot boot ../boot_test.img          # 临时引导测试, 不改分区
    # 或
    fastboot flash boot_b ../boot_test.img  # 刷入当前槽位 (见 fastboot getvar current-slot)

设备 A/B 分区, vbmeta 无锁无校验。


模块兼容性
----------

与原厂 vendor 模块 ABI 兼容 (上机实测 WiFi/蓝牙/GPS 全部工作):

- vermagic 精确一致: 4.14.186-g08d3400dc4c7-dirty SMP preempt mod_unload modversions aarch64
- vendor 模块 (wlan_drv_gen3/bt/gps/fmradio/fpsgo/met/kheaders) 由原厂内核编译,
  其 MODVERSIONS 符号 CRC 记录的是原厂值, 无法与我们内核匹配 (task_struct 等
  结构体因 KernelSU/SUSFS 增加了字段, 类型 AST 变化导致导出符号 CRC 变化)。
  因此 kernel/module.c 的 check_version/check_modstruct_version 已放行为无条件通过
  (vermagic 检查保留); 运行时布局兼容性已实测验证。
- 缺失的 mtk_wcn_* connsys 符号由原厂 wmt_drv.ko 自导出。


各部件移植/修复说明
-------------------

显示         正常   根因是 dsi.mode 误用 BURST_VDO_MODE; 经反汇编原厂内核 get_params
                    提取真实时序参数 (SYNC_PULSE_VDO_MODE, cont_clock=1, vsa=8/vbp=28/
                    vfp=103, hsa=14/hbp=28/hfp=42, PLL=746), 并用从 LK 二进制提取的
                    81 条原厂 init 序列初始化面板。

背光/息屏唤醒  正常   resume 崩溃根因: ktz8864a 电源/背光 IC 无 dts 节点导致 i2c 客户端
                    为 NULL, resume 时 ktz8864a_write_byte -> i2c_master_send panic。
                    已加 NULL 保护 (LK 已初始化电源, resume 跳过写)。

WiFi/蓝牙/GPS/FM 正常  使用原厂 vendor 模块 (见上)。

电池/充电     正常   MT6358 gauge/charger + 板级 battery profile (dtb)。

相机         内核侧正常  s5k3l6_mipi_raw 传感器驱动 (Micromax 版适配 v1_1 API),
                    camera_main/sub 在 i2c 上探测并绑定 kd_camera_hw。
                    s5k3l7 用占位 stub (无公开源码)。相机 HAL 是 GSI userspace 问题。

触摸         正常   见下节。


触摸 (HX83102P incell, SPI)
--------------------------

正常工作。芯片检测 (hx83102e)、1600x2176 坐标、触摸固件下载
(/vendor/firmware/Himax_firmware_spi.bin, 版本 0xD204 与原厂一致)、IRQ18 注册、
IC 信息读取均正常, 触摸寄存器配置与原厂逐项一致。


对源码树所做的修改 (相对上游)
-----------------------------

1.  顶层 Makefile: 默认 MTK_PLATFORM=mt6771 + MTKINCLUDES 全局 include 路径
    (alps 跨模块尖括号引用; 542 个 MTK Makefile 依赖 $(MTK_PLATFORM))。
2.  顶层无 Kconfig (MTK 惯例): 构建需 KBUILD_KCONFIG=arch/arm64/Kconfig。
3.  补齐上游漏传的 init/Kconfig 及 408 个缺失 Kconfig (从 gregkh/linux v4.14.186 恢复)。
4.  drivers/devfreq/Makefile, drivers/extcon/mediatek/Makefile,
    drivers/mmc/host/mediatek/ComboA/Makefile: 补 -I 本地头文件路径。
5.  drivers/misc/mediatek/accdet/mt6358/accdet.c: 修复条件编译语法错误 + 未用变量。
6.  drivers/devfreq/helio-dvfsrc.c: 修正笔误的尖括号 include。
7.  LCM: 新建 hx83102p_2082110afh036002_53c 与 tv110xum_lb2_incell_v2 面板驱动
    (同 IC 模板适配, 1600x2176, 注册进 lcm_driver_list), .name 去后缀以匹配 LK 上报;
    get_params 用反汇编提取的原厂时序; init 用 LK 提取的 81 条序列。
8.  传感器: s5k3l6_mipi_raw (去 OPPO 依赖/适配 tag 风格 enum/删 SensorModuleName/OtpID);
    s5k3l7_mipi_raw 占位 stub。
9.  触摸: 移植 hxchipset_spi (GS290-dev) + mtk_tpd 框架补 elink,touch 匹配 +
    SPI 引脚功能配置 (_h 状态) + mediatek,spi_himax_touch of_match + 中断从 SPI
    设备节点解析 + 坐标改 1600x2176 + 固件名改 Himax_firmware_spi.bin + GTP_RST_PORT=158。
10. drivers/input/touchscreen/mediatek/mtk_tpd.c: touch_of_match 增加 elink,touch。
11. KernelSU (SukiSU Ultra) 集成 + SUSFS v2.2.0 移植 (见下)。
12. kernel/module.c: vendor 模块符号 CRC 检查放行 (见"模块兼容性")。


KernelSU (SukiSU Ultra) 集成
----------------------------

- 驱动: SukiSU Ultra main (driver 40877), 4.14 全面适配
  (LSM/patch_memory/selinux/file_wrapper/fsnotify 等 compat + path_mount/path_umount backport)。
- SUSFS v2.2.0 全部 13 项特性开启: sus_path / sus_mount / sus_kstat / spoof_uname /
  spoof_cmdline / open_redirect / sus_map / try_umount / sus_su (非 kprobe 版) /
  sdcard_monitor / avc_log_spoofing / hide_sus_mnts / enable_log。
  TASK_STRUCT_NON_ROOT_USER_APP_PROC 位由 exec 时 uid>=10000 设置。
- 4.14 关键适配: MTK el0_svc 直接 blr (syscall 表参数版) -> ksu_syscall_table_call()
  wrapper; 4.14 无 seccomp action-cache -> disable_seccomp() 路径;
  CONFIG_FTRACE_SYSCALLS=y 必须开; reboot/input kprobe handler 用直接参数。
- Manager 显示 v4.1.3-TALIH-PD1@Fumor (KSU_VERSION_FULL 在 Kbuild 写死)。


已知次要问题
------------

- hx83102p_fhdp_dsi_vdo_boe platform driver 名在两个面板拷贝中重复
  (第二次注册 abort, 不影响主面板)。
- s5k3l7 相机传感器无公开源码 (占位 stub)。
- 元模块 (Hybrid Mount/mountify/meta-overlayfs) 与 GSI 兼容性受限: overlay 依赖
  /system/bin symlink (GSI 为真目录) 或 5.x 内核 API; 目录型固化模块 (gms/vending/
  hwid 的 priv-app) 由 KSU 内核 overlay 挂载生效, 单文件模块走 Hybrid Mount magic。


参考资料
--------

- 公开固件 dump: r0rt1z2-dumpyard/chuwi_hipadplus_dump (同板 tb8788p1_64_wifi,
  含 boot/vendor/dtbo, 可提取触摸固件与 dtb)。
- Himax 官方参考: HimaxSoftware/HX83112_Android_Driver (incell 实现参考)。
